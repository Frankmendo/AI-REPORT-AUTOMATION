services:
  - type: web
    name: ai-report-automation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: HF_API_KEY
        sync: false